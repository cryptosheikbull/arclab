import React from 'react';

const Header = () => {
  return (
    <header style={{ backgroundColor: '#002B5C', color: '#00A3FF', padding: '15px', textAlign: 'center', fontWeight: 'bold', borderBottom: '2px solid #003366', borderRadius: '0 0 15px 15px' }}>
      This DApp is not official from the ARC project and has no affiliation with it.
    </header>
  );
};

export default Header;